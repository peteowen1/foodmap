% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrape_infatuation.R
\name{scrape_infatuation}
\alias{scrape_infatuation}
\title{Scrape The Infatuation's editorial guides}
\usage{
scrape_infatuation(
  city = "san-francisco",
  extra_guides = character(),
  use_cache = FALSE
)
}
\arguments{
\item{city}{Character. Lowercase city slug. Currently supported:
\code{"san-francisco"}. Default \code{"san-francisco"}.}

\item{extra_guides}{Character vector. Additional guide slugs to
fetch on top of the flagship list. Default \code{character()}.}

\item{use_cache}{Logical. If \code{TRUE}, cache each guide page locally
(24-hour TTL via \code{cached_fetch()}). Default \code{FALSE}.}
}
\value{
A tibble with columns: name, suburb, address, cuisine,
category, description, price_range, rating, rating_scale, latitude,
longitude, url.
}
\description{
The Infatuation publishes city-by-city editorial guides ("21
Restaurants To Make You Fall In Love With SF Again", "Best Italian
Restaurants in SF", etc.). Each guide page embeds a structured
JSON-LD ItemList with the full venue details (name, address,
coordinates, cuisine, price, description), so a single HTTP fetch
per guide is enough — no per-venue detail call needed.
}
\details{
By default we pull the flagship "best of" list per city. Pass a
character vector of additional guide slugs (e.g. \code{"best-italian- restaurants-san-francisco"}) via \code{extra_guides} to broaden
coverage across cuisines / occasions / categories.
}

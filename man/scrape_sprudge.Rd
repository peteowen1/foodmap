% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrape_sprudge.R
\name{scrape_sprudge}
\alias{scrape_sprudge}
\title{Scrape Sprudge SF coffee shop spotlights}
\usage{
scrape_sprudge(
  city = "san-francisco",
  extra_guides = character(),
  use_cache = FALSE
)
}
\arguments{
\item{city}{Character. Supported cities are \code{"san-francisco"},
\code{"new-york"}, \code{"los-angeles"}, \code{"london"}, \code{"sydney"}, and
\code{"melbourne"}. Default \code{"san-francisco"}.}

\item{extra_guides}{Character vector of extra Sprudge article URLs
to harvest on top of the search-driven defaults. Useful for
one-off spotlight articles that don't match the default search
queries (e.g. founder profiles, expansion stories).}

\item{use_cache}{Logical. Cache HTTP responses for 24h via
\code{cached_fetch()}. Default \code{FALSE}.}
}
\value{
A tibble with the standard scraper schema. Address /
latitude / longitude are \code{NA} -- they get filled in downstream by
\code{geocode_restaurants()}.
}
\description{
Sprudge is a specialty-coffee publication that covers individual
cafes through a few recurring article series ("Coffee Design",
"Sprudge Maps Spotlight"). Unlike Eater/Infatuation, there is no
single roundup page with an embedded venue list -- each cafe gets
its own article. This scraper harvests cafe names by querying
Sprudge's WordPress search for SF terms, filtering returned URLs to
the venue-spotlight patterns, and pulling the cafe name from each
article's \code{og:title} meta tag.
}
\details{
Addresses and coordinates are intentionally left blank -- Sprudge
rarely publishes structured location data and the geocoding pipeline
resolves "\if{html}{\out{<name>}} San Francisco" to coords via Google Places.
Cuisine is set to \code{"Coffee"} for every venue so the cafe filter on
the rendered map picks them up without further tagging.
}

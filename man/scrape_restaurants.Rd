% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrape_restaurants.R
\name{scrape_restaurants}
\alias{scrape_restaurants}
\title{Scrape restaurants from a specified source}
\usage{
scrape_restaurants(
  city = "sydney",
  source = "broadsheet",
  use_chromote = FALSE,
  use_cache = FALSE,
  use_parsed_cache = TRUE
)
}
\arguments{
\item{city}{Character. City to scrape. Default \code{"sydney"}.}

\item{source}{Character. Source to scrape from. One of \code{"broadsheet"},
\code{"gourmet_traveller"}, \code{"timeout"}, \code{"urban_list"}, \code{"agfg"},
\code{"good_food_guide"}, \code{"gfg_awards"}, \code{"concrete_playground"},
\code{"infatuation"}, \code{"eater"}, \code{"7x7"}, \code{"cn_traveler"},
\code{"james_beard"}, \code{"michelin"}, \code{"sprudge"}, \code{"thrillist"},
\code{"honolulu_magazine"}, \code{"hale_aina"}, \code{"resy"}, \code{"bonappetit"},
\code{"worlds50best"}. Default \code{"broadsheet"}.}

\item{use_chromote}{Logical. Force headless Chrome rendering where applicable.
Default \code{FALSE}.}

\item{use_cache}{Logical. If \code{TRUE}, cache HTTP responses locally to avoid
re-fetching during development. Cached responses expire after 24 hours.
Default \code{FALSE}.}

\item{use_parsed_cache}{Logical. If \code{TRUE} (and \code{use_cache} is also
\code{TRUE}), wrap the scrape in \code{cached_scrape()} so the parsed tibble
itself is cached in \verb{cache/parsed/\{source\}_\{city\}.rds}. Invalidates
automatically when any underlying HTML cache file changes. Skipped
when \code{use_cache = FALSE} (the user explicitly wants fresh data).
Default \code{TRUE}.}
}
\value{
A tibble with columns: name, suburb, address, cuisine, category,
description, price_range, rating, rating_scale, latitude, longitude, url.
}
\description{
Unified dispatcher that calls the appropriate scraper for the given source.
All scrapers return a tibble with the same schema.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrape_michelin.R
\name{scrape_michelin}
\alias{scrape_michelin}
\title{Scrape the Michelin Guide}
\usage{
scrape_michelin(
  city = "san-francisco",
  use_cache = FALSE,
  max_age_hours = 24 * 30
)
}
\arguments{
\item{city}{Character. Lowercase city slug. Currently supported:
\code{"san-francisco"}. Default \code{"san-francisco"}.}

\item{use_cache}{Logical. Cache HTTP responses on disk. Default
\code{FALSE}.}

\item{max_age_hours}{Numeric. Cache freshness window in hours when
\code{use_cache = TRUE}. Default \code{24 * 30} (30 days).}
}
\value{
A tibble with the standard scraper schema plus
\code{michelin_distinction} (one of "Three Stars", "Two Stars",
"One Star", "Bib Gourmand", "Selected"), \code{michelin_year},
\code{phone}, and \code{cost_range} columns.
}
\description{
Pulls the full restaurant set for a city — every tier from Selected
through Bib Gourmand and One/Two/Three Stars. The listing pages are
server-rendered HTML with a flat link-card layout, and detail pages
embed a clean \code{Restaurant} JSON-LD block (address, latitude/longitude,
cuisine, star rating, award year, telephone, description). No
headless browser required.
}
\details{
Michelin sits behind CloudFront and silently bounces requests with a
bare User-Agent (HTTP 202 with empty body), so the scraper layers in
\code{Accept}, \code{Accept-Language}, and \code{Referer} headers. The Guide
refreshes annually, so caching defaults to 30 days rather than the
usual 24 hours — re-running with \code{use_cache = TRUE} finishes in
seconds and never re-fetches.
}

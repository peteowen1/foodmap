% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrape_resy.R
\name{scrape_resy}
\alias{scrape_resy}
\title{Scrape Resy's monthly Hit List per city}
\usage{
scrape_resy(city = "new-york", use_cache = FALSE)
}
\arguments{
\item{city}{Character. Lowercase city slug. Currently supported:
\code{"new-york"}, \code{"los-angeles"}. Default \code{"new-york"}.}

\item{use_cache}{Logical. Cache the Hit List page for 24h via the
package's internal \code{cached_fetch()} helper. Default \code{FALSE}.}
}
\value{
A tibble with the standard scraper schema. Latitude /
longitude come straight from the page DOM so no geocoder call is
needed for these rows.
}
\description{
Resy maintains a rolling, monthly-updated "Hit List" of the top
restaurants in each city it covers (NYC, LA, plus historical lists
for other cities). The article HTML is dense with structured data:
every venue is rendered as an \verb{<article class="teaser2">} block
with \code{data-lat} / \code{data-lng} attributes on the article tag, the
venue name + rank inside an \verb{<h3 class="teaser2-title">}, and a
\verb{<ul class="teaser2-meta">} carrying the neighborhood, cuisine and
price tier as three \verb{<li>} items.
}
\details{
That means a single fetch yields name, neighborhood, cuisine,
price, rank, coordinates and the Resy booking URL - no per-venue
detail fetch needed (the per-venue pages are Angular-rendered
anyway, so they'd require a headless browser).
}

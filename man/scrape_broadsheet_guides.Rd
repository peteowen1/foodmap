% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrape_broadsheet_guides.R
\name{scrape_broadsheet_guides}
\alias{scrape_broadsheet_guides}
\title{Scrape Broadsheet's editorial cafe / bar / pub guides}
\usage{
scrape_broadsheet_guides(city = "sydney", use_cache = FALSE)
}
\arguments{
\item{city}{Character. \code{"sydney"} or \code{"melbourne"}. Default
\code{"sydney"}.}

\item{use_cache}{Logical. Cache HTTP responses for 24h via
\code{cached_fetch()}. Default \code{FALSE}.}
}
\value{
A tibble with the standard scraper schema. \code{category} is
set from the JSON-LD \verb{@type} (\code{"Cafe"}, \code{"Bar"}, or \code{"Restaurant"}).
}
\description{
Complements \code{scrape_broadsheet()} (which only hits the restaurant
hotlist API). Broadsheet's \verb{/\{city\}/guides/\{slug\}} pages are curated
editorial best-of lists for cafes, bars, cocktail spots, pubs, etc.
Each guide page embeds a \code{schema.org} \code{ItemList} JSON-LD block whose
\code{itemListElement} array carries clean venue records:
}
\details{
\itemize{
\item \verb{@type}      - \code{CafeOrCoffeeShop} / \code{BarOrPub} / \code{Restaurant}
\item \code{name}, \code{url}, \code{description}, \code{image}, \code{telephone}
\item \code{priceRange} (integer 1-4)
\item \code{address}    - \code{streetAddress}, \code{addressLocality} (suburb),
\code{postalCode}, \code{addressRegion}
}

That's everything the standard pipeline schema needs; no per-venue
detail fetch is required.

Coverage is curated, not enumerated - each city has a hand-picked
list of cafe + bar guides. Discoverable via
\verb{https://www.broadsheet.com.au/sitemap/\{city\}/guides} if a new
guide is published and you want to fold it in.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression_check.R
\name{assert_venue_count}
\alias{assert_venue_count}
\title{Assert the mapped venue count for a city is not a major regression}
\usage{
assert_venue_count(
  restaurants,
  city,
  expected_counts_path = NULL,
  require = TRUE
)
}
\arguments{
\item{restaurants}{Tibble. The geocoded/harmonised pipeline output.}

\item{city}{Character. City slug used to look up the row.}

\item{expected_counts_path}{Character or \code{NULL}. Optional path to a
custom expected-counts CSV. Defaults to \code{NULL}, which means:
prefer \code{inst/extdata/expected_counts.csv} in the current working
directory if present (live dev / test fixtures), otherwise fall
back to the installed package's \code{system.file()} copy.}

\item{require}{Logical. If \code{TRUE} (default), missing-city entry
triggers a soft warning and the check is skipped. The pipeline
continues so a city without a baseline isn't blocked from
running.}
}
\value{
The input tibble unchanged (suitable for piping). Aborts
on regression.
}
\description{
Reads \code{inst/extdata/expected_counts.csv} and looks up \code{city}'s
\code{expected_min}. If the supplied \code{restaurants} tibble has fewer
georeferenced venues (rows with non-NA latitude) than that minimum,
the function aborts with a clear message naming both numbers.
}
\details{
The intent is to catch silent breakage in the upstream sites: a
scraper continues to run (no error, no warning) but suddenly
returns 50 venues instead of 600. By default this isn't visible -
the CSV / KML / HTML still get written. Wiring this assertion into
each analysis script makes the pipeline fail loudly when that
happens.

Expected counts are kept under \verb{inst/extdata/} (not a package
constant) so the maintainer can edit them without a code change.
The CSV has three columns: \code{city}, \code{expected_min}, \code{note}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrape_thrillist.R
\name{scrape_thrillist}
\alias{scrape_thrillist}
\title{Scrape Thrillist's "Best Restaurants in CITY" editorial roundup}
\usage{
scrape_thrillist(
  city = "honolulu",
  extra_guides = character(),
  use_cache = FALSE
)
}
\arguments{
\item{city}{Character. Lowercase city slug. Currently supported:
\code{"honolulu"}. Default \code{"honolulu"}.}

\item{extra_guides}{Character vector. Extra Thrillist article URLs
to fetch alongside the city's default best-restaurants page.
Default \code{character()}.}

\item{use_cache}{Logical. Cache responses for 24h via the package's
internal \code{cached_fetch()} helper. Default \code{FALSE}.}
}
\value{
A tibble with the standard scraper schema.
}
\description{
Thrillist publishes a single canonical "Best Restaurants" article
per city (refreshed periodically). Each venue gets an
\verb{<h2 class="...LocationListItemTitle...">} heading followed by a
paragraph of editorial prose, and the same article embeds an
inline \code{Restaurant} JSON-LD block per venue with
\code{GeoCoordinates} (latitude / longitude). No detail page fetch is
required.
}
\details{
The JSON-LD blocks don't include \code{address} or \code{servesCuisine}, so
we infer cuisine from the editorial description via the shared
\code{prose_to_cuisine()} helper and leave address blank for the
geocoder to resolve (or, in practice, ignore - we already have
coordinates).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrape_honolulu_magazine.R
\name{scrape_honolulu_magazine}
\alias{scrape_honolulu_magazine}
\title{Scrape HONOLULU Magazine editorial restaurant roundups}
\usage{
scrape_honolulu_magazine(
  city = "honolulu",
  extra_guides = character(),
  use_cache = FALSE
)
}
\arguments{
\item{city}{Character. Currently \code{"honolulu"}. Default
\code{"honolulu"}.}

\item{extra_guides}{Character vector. Extra article URLs to fetch
on top of the default editorial set. Default \code{character()}.}

\item{use_cache}{Logical. Cache responses for 24h. Default \code{FALSE}.}
}
\value{
A tibble with the standard scraper schema.
}
\description{
HONOLULU Magazine is the city's monthly print/digital magazine and
publishes a steady cadence of editorial "best of" roundups. This
scraper handles two staple articles:
}
\details{
\itemize{
\item "Best Restaurants in Honolulu" - the rolling editorial picks
maintained by Mari Taketa, Thomas Obungen and Melissa Chang.
Each venue is anchored with \verb{<h1 style="text-align: center;">}.
\item "Oʻahu's Best New Restaurants of YYYY" - the annual cohort
article that profiles 5-10 new openings. Each venue is anchored
with a bare \verb{<h2>}.
}

The two layouts are similar enough to share a parser:
a venue heading followed by one or more \verb{<p>} paragraphs of
editorial prose. The scraper auto-detects which heading level
anchors venues by counting candidates at each level and picking
the level with more than one match (so a single page H1 doesn't
falsely register as a venue heading).

Hale ʻAina Awards live in a separate scraper
(\code{scrape_hale_aina()}) - that page has a category/rank shape that
doesn't fit the venue-heading pattern.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_diagnostics.R
\name{export_diagnostics}
\alias{export_diagnostics}
\title{Export a diagnostics CSV listing rows with missing data}
\usage{
export_diagnostics(
  restaurants,
  output_path,
  fields = c("latitude", "address", "description", "price_range", "cuisine", "url")
)
}
\arguments{
\item{restaurants}{A tibble as returned by \code{harmonize_sources()} or
any pipeline stage with the standard schema.}

\item{output_path}{Character. File path for the diagnostics CSV.}

\item{fields}{Character vector of column names to consider "key".
Default \code{c("latitude", "address", "description", "price_range", "cuisine", "url")}. Pass a subset to focus on a single class of
issue (e.g. only geocoding misses).}
}
\value{
The output path (invisibly), or \code{NULL} when there are no
issues to report.
}
\description{
Surfaces the rows in a pipeline output that have one or more
underspecified fields - typically the ones that fall out of the KML
or render as bare popups in the HTML map. Writes a CSV with two
extra columns:
}
\details{
\itemize{
\item \code{issues} - comma-separated list of fields that are blank for
this row (e.g. "latitude, description").
\item \code{issue_count} - number of fields blank, used to sort worst-first.
}

Pipe the resulting CSV into manual fix decisions: either add a row
to \code{inst/extdata/manual_excludes.csv}, or correct the cache entry
directly, or open a scraper-improvement issue.
}

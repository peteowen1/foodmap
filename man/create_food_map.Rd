% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_food_map.R
\name{create_food_map}
\alias{create_food_map}
\title{Create a food map from restaurant guide sources}
\usage{
create_food_map(
  city = "sydney",
  source = "broadsheet",
  output_dir = ".",
  api_key = NULL,
  use_chromote = FALSE
)
}
\arguments{
\item{city}{Character. City to scrape. Default \code{"sydney"}.}

\item{source}{Character. Source to scrape from. One of \code{"broadsheet"},
\code{"gourmet_traveller"}, \code{"timeout"}, \code{"urban_list"}, \code{"agfg"},
\code{"good_food_guide"}. Default \code{"broadsheet"}.}

\item{output_dir}{Character. Directory for output files. Created if needed.
Defaults to the current working directory.}

\item{api_key}{Character. Google Places API key. Defaults to the
\code{GOOGLE_PLACES_API_KEY} environment variable.}

\item{use_chromote}{Logical. Force headless Chrome rendering.
Default \code{FALSE} (auto-falls back if needed).}
}
\value{
A tibble of geocoded restaurants (invisibly).
}
\description{
One-call pipeline: scrapes a restaurant guide, geocodes venues
missing coordinates via Google Places API, and exports KML + CSV files
ready for import into Google My Maps.
}
\examples{
\dontrun{
Sys.setenv(GOOGLE_PLACES_API_KEY = "your-key")
create_food_map("sydney", output_dir = "~/maps")
create_food_map("sydney", source = "timeout", output_dir = "~/maps")
}
}

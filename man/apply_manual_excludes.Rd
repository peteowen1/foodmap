% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exclusions.R
\name{apply_manual_excludes}
\alias{apply_manual_excludes}
\title{Apply manually-curated venue exclusions}
\usage{
apply_manual_excludes(restaurants, city = NULL)
}
\arguments{
\item{restaurants}{Tibble with at least \code{name} and (typically)
\code{suburb} and \code{source} columns.}

\item{city}{Character or \code{NULL}. The current city slug; matches
rows whose \code{city} value equals this or \code{"*"}. When \code{NULL}, only
\code{"*"} (global) exclusions apply.}
}
\value{
The input tibble with matching rows removed.
}
\description{
Reads \code{inst/extdata/manual_excludes.csv} and drops any rows matching
the curated exclusion records. Use this to remove venues that are
stale (closed, renamed), wrongly scraped (cross-region listings),
or otherwise problematic - without having to patch the upstream
scraper.
}
\details{
Matching rules:
\itemize{
\item \code{name} is required and matched case-insensitively.
\item \code{city} is required. Pass \code{"*"} to apply across every city.
\item \code{suburb} is optional - when present, both name and suburb must
match (also case-insensitive). Leave blank to match the name
regardless of suburb (useful for venues that appear with various
suburbs across guides).
\item \code{source} is optional - when present, restricts the exclusion to
rows from that source. Useful when a venue should drop from one
scraper's pull but not others.
}

The exclusion CSV has columns: \code{city}, \code{name}, \code{suburb}, \code{source},
\code{reason}, \code{date_added}. The \code{reason} and \code{date_added} columns are
for the maintainer; they have no effect on matching.
}

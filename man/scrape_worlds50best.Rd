% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrape_worlds50best.R
\name{scrape_worlds50best}
\alias{scrape_worlds50best}
\title{Scrape The World's 50 Best Restaurants}
\usage{
scrape_worlds50best(
  city = "new-york",
  use_cache = FALSE,
  max_age_hours = 24 * 30
)
}
\arguments{
\item{city}{Character. Lowercase city slug used by the geocoder.
The scraper resolves it to one or more "locality" strings that
W50B uses in the JSON-LD address field. Default \code{"new-york"}.}

\item{use_cache}{Logical. Cache pages on disk. Default \code{FALSE}.}

\item{max_age_hours}{Numeric. Cache freshness window when
\code{use_cache = TRUE}. Default \code{24 * 30} (30 days - the list
refreshes annually).}
}
\value{
A tibble with the standard scraper schema, plus an
\code{award_year} integer column (the year the venue won/placed).
}
\description{
W50B publishes a server-rendered ranked list (1-50 + 51-100) of
the world's top restaurants. Each list page is just a grid of
\verb{<div class="list-item">} cards carrying rank, venue name, city
and a link to a detail page; each detail page embeds clean
\code{Restaurant} JSON-LD with the street address, founder/chef, award
citations and a short description.
}
\details{
City filtering happens by name match against the detail page's
\code{addressLocality} - so an \code{"new-york"} scrape keeps only the NYC
entries, drops Tokyo, Lima, Barcelona, etc. Geocoding fills in
coordinates afterward (no lat/lng in the JSON-LD).

Detail pages are cheap to cache (the awards change once a year)
so \code{max_age_hours} defaults to 30 days when caching is enabled.
}

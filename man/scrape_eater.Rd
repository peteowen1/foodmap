% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrape_eater.R
\name{scrape_eater}
\alias{scrape_eater}
\title{Scrape Eater's "Essential X" + map guides}
\usage{
scrape_eater(
  city = "san-francisco",
  extra_guides = character(),
  use_cache = FALSE
)
}
\arguments{
\item{city}{Character. Lowercase city slug. Currently supported:
\code{"san-francisco"}. Default \code{"san-francisco"}.}

\item{extra_guides}{Character vector of additional map slugs to
fetch alongside the flagship list. Default \code{character()}.}

\item{use_cache}{Logical. Cache page responses for 24h. Default \code{FALSE}.}
}
\value{
A tibble with the standard scraper schema.
}
\description{
Eater publishes city-by-city map guides ("The 38 Best Restaurants in
San Francisco", "Heatmap of New Restaurants", "Best Bars", etc.).
Each map page embeds the venue payload as inline JSON inside the
article HTML (one block per venue with \code{location.latitude},
\code{location.longitude}, \code{name}, \code{venue.address}). We pull those out
with a couple of regexes — no per-venue detail fetch needed.
}
\details{
By default the scraper hits the city's flagship "Essential 38" /
"best restaurants" guide. Pass additional map slugs via
\code{extra_guides} to broaden across categories (best brunch, best
bars, best coffee, etc.).
}

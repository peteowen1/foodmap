% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrape_bonappetit.R
\name{scrape_bonappetit}
\alias{scrape_bonappetit}
\title{Scrape Bon Appétit's annual "Best New Restaurants in America" list}
\usage{
scrape_bonappetit(
  city = "new-york",
  extra_guides = character(),
  use_cache = FALSE
)
}
\arguments{
\item{city}{Character. Lowercase city slug (e.g. \code{"new-york"},
\code{"los-angeles"}, \code{"san-francisco"}, \code{"honolulu"}). Default
\code{"new-york"}.}

\item{extra_guides}{Character vector. Extra BA article URLs to
fetch alongside the default. Default \code{character()}.}

\item{use_cache}{Logical. Cache page responses for 24h. Default
\code{FALSE}.}
}
\value{
A tibble with the standard scraper schema. Address and
coordinates are \code{NA} - the geocoder fills them in.
}
\description{
Bon Appétit publishes one annual article (~20 venues) spanning every
major US city. The 2025 article renders each venue as a pair of
\verb{<strong>} blocks - the venue name followed by \verb{<strong>CITY |</strong>}
(e.g. "NEW YORK CITY |", "WASHINGTON, DC |") - which is a stable
enough shape to parse without per-venue detail pages.
}
\details{
Bon Appétit doesn't expose price, cuisine, address, or coordinates on
the list page (each venue links to a longer review article). We
pre-filter by city label so a Honolulu run doesn't burn API quota
geocoding 20 Pittsburgh / Atlanta venues; the geocoder then resolves
the survivors to coordinates.

The 2024 article uses a different (Gatsby-rendered) layout - if you
want that year too, pass its URL via \code{extra_guides} and the parser
will fall through to the H2-only branch (city info won't be
available for those, so the geocoder gets a name-only query).
}
